{
  "name": "my-examples",
  "version": "1.0.0",
  "description": "Vacation checker",
  "main": "src/server.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf build/ && prettier --write source/ && tsc",
    "dev": "nodemon source/server.ts",
    "start": "node source/server.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/express": "^4.17.11",
    "nodemon": "^2.0.7"
  },
  "dependencies": {
    "@flumens/fetch-onedrive-excel": "^0.1.1",
    "dotenv": "^8.2.0",
    "express": "^4.17.1"
  }
}
